{
  "name": "@the-dmz/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('build not configured for web yet')\"",
    "dev": "node -e \"console.log('dev not configured for web yet')\"",
    "lint": "node -e \"console.log('lint not configured for web yet')\"",
    "test": "node ./scripts/verify-shared-import.mjs",
    "typecheck": "node -e \"console.log('typecheck not configured for web yet')\"",
    "clean": "node -e \"console.log('clean not configured for web yet')\""
  },
  "dependencies": {
    "@the-dmz/shared": "workspace:*"
  }
}
