{
  "name": "@the-dmz/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('build not configured for api yet')\"",
    "dev": "node -e \"console.log('dev not configured for api yet')\"",
    "lint": "node -e \"console.log('lint not configured for api yet')\"",
    "test": "node ./scripts/verify-shared-import.mjs",
    "typecheck": "node -e \"console.log('typecheck not configured for api yet')\"",
    "clean": "node -e \"console.log('clean not configured for api yet')\""
  },
  "dependencies": {
    "@the-dmz/shared": "workspace:*"
  }
}
